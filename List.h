#pragma once

#include <stdbool.h>

typedef struct Node{

}Node;
int addHead(Node **node);
int add(Node **head);
int delete(Node **node);
int deleteHead(Node **head);
bool isEmpty(Node **head);
int clear(Node **head);

